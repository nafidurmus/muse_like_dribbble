- @posts.each do |post|
	= link_to (image_tag post.image.url(:thumb))
	%h2= link_to post.title, post
	%p
		= post.comments.count
		Comments
	%p
		= post.get_likes.size
		likes

= link_to "Add new ınsiration", new_post_path(@post)